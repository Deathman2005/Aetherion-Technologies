{
  "name": "aetherion-technologies",
  "version": "1.0.0",
  "description": "Premium Enterprise Website for Aetherion Technologies",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build:all": "npm run build --prefix frontend && npm run build --prefix backend",
    "start:all": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
